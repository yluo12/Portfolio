import ProjectCard from '@components/ProjectCard';

const Projects = () => {
  const projectData = {

  };

  return (
    <section>
      <h2>PROJECTS</h2>
      <ProjectCard />
    </section>
  )
};

export default Projects;